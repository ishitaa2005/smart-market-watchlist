# Placeholder — SQLAlchemy engine, SessionLocal, and get_db() dependency
# will be added here in the next step, once models are defined.
#
# Planned shape:
#   engine = create_engine(settings.database_url)
#   SessionLocal = sessionmaker(bind=engine)
#   def get_db(): ...
